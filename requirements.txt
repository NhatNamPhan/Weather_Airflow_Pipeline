# Open-Meteo Weather API
openmeteo-requests
requests-cache
retry-requests

# Data Processing
pandas>=2.0
numpy

# Database
sqlalchemy>=2.0
psycopg2-binary

# Environment variables
python-dotenv

# Streamlit (pinned for compatibility)
streamlit>=1.32.0
plotly>=5.18.0
altair<5  # Ensure compatibility with Streamlit
